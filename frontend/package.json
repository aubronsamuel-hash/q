{
  "name": "project-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "e2e": "PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 PW_CHROMIUM_PATH=${PW_CHROMIUM_PATH:-/usr/bin/google-chrome-stable} playwright test",
    "e2e:ui": "PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 PW_CHROMIUM_PATH=${PW_CHROMIUM_PATH:-/usr/bin/google-chrome-stable} playwright test --ui",
    "test:unit": "react-scripts test --watchAll=false"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.23.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@testing-library/jest-dom": "^6.7.0",
    "@testing-library/react": "^14.3.1",
    "react-scripts": "5.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
